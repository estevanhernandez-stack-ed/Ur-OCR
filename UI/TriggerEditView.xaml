<UserControl x:Class="RoRoRo.UrOcr.UI.TriggerEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RoRoRo.UrOcr.UI">
    <ScrollViewer>
        <StackPanel Margin="8">
            <TextBlock Text="Name" />
            <TextBox Text="{Binding Source.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,8"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <CheckBox Content="Enabled" IsChecked="{Binding Enabled}" Margin="0,0,16,0"/>
                <CheckBox Content="Account-aware" IsChecked="{Binding Source.AccountAware}"/>
            </StackPanel>

            <TextBlock Text="Region" />
            <TextBlock Text="{Binding Source.Region}" Margin="0,2,0,4"/>
            <Button Content="Re-pick region" HorizontalAlignment="Left" Padding="6,3" Margin="0,0,0,8"/>

            <TextBlock Text="Keybind" />
            <local:KeybindCapture Combo="{Binding Source.Keybind, Mode=TwoWay}" Margin="0,2,0,8"/>

            <TextBlock Text="Cooldown (ms)" />
            <TextBox Text="{Binding Source.CooldownMs}" Margin="0,2,0,8"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
