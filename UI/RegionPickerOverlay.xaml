<Window x:Class="RoRoRo.UrOcr.UI.RegionPickerOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RoRoRo.UrOcr.UI"
        WindowStyle="None" AllowsTransparency="True" Background="#80000000"
        Topmost="True" Cursor="Cross" WindowStartupLocation="Manual"
        ShowInTaskbar="False">
    <Canvas x:Name="Root" Background="Transparent"
            MouseLeftButtonDown="OnDown" MouseMove="OnMove" MouseLeftButtonUp="OnUp"
            KeyDown="OnKeyDown" Focusable="True">
        <Rectangle x:Name="Selection" Stroke="#22DDFF" StrokeThickness="2" Fill="Transparent"
                   Visibility="Collapsed"/>
        <local:Loupe x:Name="LoupeCtl" Visibility="Collapsed"/>
        <TextBlock x:Name="ReadOut" Foreground="White" FontSize="13" Margin="10"
                   Canvas.Left="10" Canvas.Top="10"/>
    </Canvas>
</Window>
