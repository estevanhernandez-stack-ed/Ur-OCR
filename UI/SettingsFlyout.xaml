<Window x:Class="RoRoRo.UrOcr.UI.SettingsFlyout"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RoRoRo.UrOcr.UI"
        Title="Settings"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BgBrush}"
        Foreground="{StaticResource WhiteBrush}"
        FontFamily="{StaticResource BodyFont}">
    <StackPanel Margin="20" MinWidth="340">

        <TextBlock Text="TICK RATE (HZ)"
                   Style="{StaticResource MetaLabel}"
                   Margin="0,0,0,4" />
        <ComboBox x:Name="TickRate" Margin="0,0,0,16">
            <ComboBoxItem Content="3" />
            <ComboBoxItem Content="5" />
            <ComboBoxItem Content="10" />
        </ComboBox>

        <TextBlock Text="PAUSE-ALL HOTKEY"
                   Style="{StaticResource MetaLabel}"
                   Margin="0,0,0,4" />
        <local:KeybindCapture x:Name="PauseHotkey" Margin="0,0,0,16" />

        <TextBlock Text="KEY HOLD (MS)"
                   Style="{StaticResource MetaLabel}"
                   Margin="0,0,0,4" />
        <TextBox x:Name="HoldMs" Margin="0,0,0,20" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Style="{StaticResource SecondaryButton}"
                    Width="90"
                    Margin="0,0,8,0"
                    Click="OnCancel"
                    IsCancel="True" />
            <Button Content="Save"
                    Style="{StaticResource PrimaryButton}"
                    Width="90"
                    Click="OnSave"
                    IsDefault="True" />
        </StackPanel>

    </StackPanel>
</Window>
